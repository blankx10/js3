# 青岩万花综合大学 · JX3 师门主页

这是一个可直接部署到 Cloudflare Pages 的静态网页项目。

## 部署步骤

1. 将当前目录内容提交到 Git 仓库。
2. 在 Cloudflare Pages 中创建项目，选择该仓库。
3. 构建设置保持默认：
   - 构建命令：留空
   - 输出目录：`/`
4. 部署完成后即可访问。

## 说明

- 项目使用原生 HTML/CSS/JavaScript，无需后端。
- 内容会保存在浏览器本地存储中，刷新后仍可保留。
- 适合静态托管平台如 Cloudflare Pages。
